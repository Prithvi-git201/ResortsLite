# ResortsLite - Cloud-Ready Configuration for GCP
spring.application.name=ResortsLite

# Dynamic port binding for Cloud Run / GKE
server.port=${PORT:8080}

# Database configuration - externalized to environment variables
spring.datasource.url=${DATABASE_URL:jdbc:h2:mem:resortdb;DB_CLOSE_DELAY=-1}
spring.datasource.username=${DATABASE_USERNAME:sa}
spring.datasource.password=${DATABASE_PASSWORD:}
spring.datasource.driver-class-name=${DATABASE_DRIVER:org.h2.Driver}

# HikariCP connection pooling configuration
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_POOL_MIN_IDLE:2}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT:600000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME:1800000}

# H2 Console (disable in production)
spring.h2.console.enabled=${H2_CONSOLE_ENABLED:false}
spring.h2.console.path=/h2-console

# Redis configuration for distributed session management (Memorystore for Redis)
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD:}
spring.redis.ssl=${REDIS_SSL:false}
spring.session.store-type=redis
spring.session.redis.namespace=resortslite:session
spring.session.timeout=${SESSION_TIMEOUT:1800}

# Google Cloud Storage configuration
gcp.storage.bucket-name=${GCS_BUCKET_NAME:resortslite-reports}
gcp.storage.project-id=${GCP_PROJECT_ID:}

# Google Cloud Secret Manager
spring.cloud.gcp.secretmanager.enabled=${SECRET_MANAGER_ENABLED:true}
spring.cloud.gcp.secretmanager.project-id=${GCP_PROJECT_ID:}

# External service endpoints - externalized to environment variables
app.payment.endpoint=${PAYMENT_SERVICE_URL:http://localhost:9090/charge}
app.inventory.endpoint=${INVENTORY_SERVICE_URL:http://localhost:8081/rooms}
app.notification.endpoint=${NOTIFICATION_SERVICE_URL:http://localhost:7070/send}

# Cloud Scheduler configuration (for replacing java.util.Timer)
app.scheduler.report-generation-cron=${REPORT_CRON:0 0 2 * * ?}

# Timezone standardization - always use UTC
spring.jackson.time-zone=UTC
