# ResortsLite - Legacy Configuration
# VIOLATION cr-java-0021: Hardcoded infrastructure values in properties file
spring.application.name=ResortsLite

# VIOLATION czr-port-001 [Software Portability]: Fixed port prevents dynamic assignment by ECS/EKS
server.port=8080

# VIOLATION cr-java-0021 [Cloud Compatibility / Mandatory]: Hardcoded DB hostname and credentials.
# Must be externalised to AWS Parameter Store or Secrets Manager for cloud deployment.
spring.datasource.url=jdbc:h2:mem:resortdb;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# VIOLATION cr-java-0021 [Cloud Compatibility / Mandatory]: Hardcoded internal service endpoints.
# These hostnames are non-routable in cloud VPC environments without manual DNS configuration.
app.payment.endpoint=http://payment-svc.internal:9090/charge
app.inventory.endpoint=http://inventory-svc.internal:8081/rooms
app.notification.endpoint=http://notify.internal:7070/send

# cz-java-0057: Externalized absolute file paths via environment variables.
# Override these via Kubernetes ConfigMap (AKS) or Azure App Configuration.
# Set REPORT_BASE_PATH and BACKUP_PATH environment variables in your container/pod spec.
app.report.base-path=${REPORT_BASE_PATH:/var/reports}
app.backup.path=${BACKUP_PATH:/var/backups/nightly}

# cz-java-0063: Azure Cache for Redis connection — credentials injected from Azure Key Vault
# via the Secrets Store CSI Driver on AKS. Set REDIS_HOST, REDIS_PORT, and REDIS_PASSWORD
# as environment variables in your pod spec (sourced from the mounted Key Vault secret).
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD:}
spring.redis.ssl=${REDIS_SSL:true}

# cz-java-0063: Enable Spring Session with Redis store. Spring Session transparently
# replaces the in-memory HttpSession with a Redis-backed distributed session, ensuring
# session state is shared across all AKS pod replicas and survives container restarts.
spring.session.store-type=redis
spring.session.redis.flush-mode=on-save
spring.session.redis.namespace=spring:session

# Health check endpoint (spring-boot-starter-actuator)
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
